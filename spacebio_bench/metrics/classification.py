"""Classification metric namespace.

The public evaluator currently implements lightweight classification metrics in
`spacebio_bench.evaluate` to avoid adding a packaging dependency. This module is
kept as the future home for reusable metric implementations as the API hardens.
"""
